apiVersion: v1
kind: Secret
metadata:
  name: project-secrets
  namespace: smap
  labels:
    app: project-api
type: Opaque
stringData:
  # PostgreSQL Configuration
  POSTGRES_USER: "project_prod"
  POSTGRES_PASSWORD: "project_prod_pwd"

  # Redis Configuration
  REDIS_PASSWORD: "21042004"

  # JWT Configuration
  JWT_SECRET_KEY: "project-jwt-secret-key-change-in-production"

  # Encrypter Configuration
  ENCRYPTER_KEY: "project-encrypt-key-32bytes-change-in-production"

  # Internal Configuration
  INTERNAL_INTERNAL_KEY: "project-internal-key-change-in-production"

  # LLM Configuration
  LLM_API_KEY: "CHANGE_ME_WITH_REAL_API_KEY"
